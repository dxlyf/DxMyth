export default MATH_CONST;
declare namespace MATH_CONST {
    let PI2: number;
    let TAU: number;
    let EPSILON: number;
    let DEG_TO_RAD: number;
    let RAD_TO_DEG: number;
    let RND: Phaser.Math.RandomDataGenerator;
    let MIN_SAFE_INTEGER: number;
    let MAX_SAFE_INTEGER: number;
}
