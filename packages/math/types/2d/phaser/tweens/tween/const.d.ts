export default TWEEN_CONST;
export namespace Phaser {
    namespace Tweens {
        /**
         * Phaser Tween state constants.
         */
        type StateType = Phaser.Tweens.States;
    }
}
declare namespace TWEEN_CONST {
    let CREATED: number;
    let DELAY: number;
    let PENDING_RENDER: number;
    let PLAYING_FORWARD: number;
    let PLAYING_BACKWARD: number;
    let HOLD_DELAY: number;
    let REPEAT_DELAY: number;
    let COMPLETE: number;
    let PENDING: number;
    let ACTIVE: number;
    let LOOP_DELAY: number;
    let COMPLETE_DELAY: number;
    let START_DELAY: number;
    let PENDING_REMOVE: number;
    let REMOVED: number;
    let FINISHED: number;
    let DESTROYED: number;
    let MAX: number;
}
