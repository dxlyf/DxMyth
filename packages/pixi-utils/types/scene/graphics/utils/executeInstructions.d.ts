import { RenderPipes } from '../../../rendering/renderers/types';
import { RenderGroup } from '../RenderGroup';
/**
 * @param renderGroup
 * @param renderer
 * @internal
 */
export declare function executeInstructions(renderGroup: RenderGroup, renderer: RenderPipes): void;
